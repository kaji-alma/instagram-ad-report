# Instagram広告レポート プロジェクト 引き継ぎ

合言葉：「引き継ぎmdを読み再開」

## これは何か
注文住宅会社A社のInstagram広告運用ポートフォリオを、単一HTMLファイルのWebレポートにまとめて
GitHub Pagesで公開している。カジさんの実績紹介用（転職・営業ポートフォリオ想定）。

- 実ファイル：[instagram_ad_report.html](../instagram_ad_report.html)（リポジトリ直下）
- GitHubリポジトリ：`kaji-alma/instagram-ad-report`（public）
- 公開URL：https://kaji-alma.github.io/instagram-ad-report/instagram_ad_report.html
- 閲覧パスワード：`mirai`（sessionStorageのクライアント側チェック。ソース閲覧・DevToolsで突破可能なことは説明済み・カジさん承知の上）
- 元データ：`~/Downloads/ファインライフ・カンパニー沖縄様　広告レポート.xlsx`（16シート、15ヶ月＋サマリー）
- デザインシステム：Ameba **Spindle**（https://spindle.ameba.design/）ベース（下記参照）

※ この working directory（`My Post`）には `my-post/` や `podcast-pipeline/` など無関係な別プロジェクトも混在している。
このドキュメントは **instagram_ad_report.html のプロジェクトのみ** を対象とする。

## 確からしさ区分について
- **検証済み**：実際にブラウザ・GitHub Pagesで表示確認、またはgit historyで確認できる事実
- **未検証**：作成・記述しただけで動作未確認
- **未解決**：次に判断・着手すべきこと

---

## 検証済み：これまでの経緯（〜フォントスタック変更まで）

1. **本体作成**：Excelデータを基にレポートHTML生成（4種SVGチャート、15ヶ月分タブ、年齢×性別グラフ等）
2. **地名の匿名化**：物件名・地名がGoogle検索で会社特定に繋がるリスクがあったため、エリア名ベースの匿名化名に置換。会社名は最初から非掲載（「注文住宅会社A社」表記）
3. **パスワード保護overlay追加**：`mirai`固定
4. **デザインリニューアル（第1弾）**（コミット `56ee5ed`）：WCAG AA準拠、ARIAタブ、セマンティックHTML、年ごとタブグループ化、印刷対応
5. **ヘッダー説明文の追加＋Claude Code注記**（コミット `8b1eef5`, `cbf81bf`）
6. **フォントスタック変更**（コミット `885feba`）：SF Pro JP → ヒラギノ角ゴ Pro → Meiryo → MS Pゴシック 系に変更
7. **「月別推移」4グラフを2列→1列レイアウトに変更**（コミット `8a414a4`）：数値ヒーローヘッダー追加、CPC/CTR数値誤差修正（CPC 15円、CTR 5.5%）。印刷用CSSは2列のまま維持
8. **却下されたデザイン案**：「ミヤマグラフィックス」風（アイボリー背景・パステル・丸ゴシック・ブロブ）を試作したが却下。詳細は下記セクション参照

（コミット `82fbc2f` までの詳細は `git log` およびコミットメッセージを参照）

---

## 検証済み：Spindleデザインへの全面移行（本セッション、★未コミット）

### 経緯
1. カジさんの希望で GitHub Primer（https://primer.style/）風モックアップをチャット内HTMLで提示 → 採用せず
2. 続けて Ameba Spindle（https://spindle.ameba.design/）風モックアップを提示 → 「Spindleで進める」と合意
3. Spindle公式サイトをブラウザで開き、`getComputedStyle(document.documentElement)` で実際のCSS変数（色・タイポグラフィ）を抽出し、**推測ではなく実測値**で移植
4. `instagram_ad_report.html` の `:root` トークンをSpindle値に置き換え、既存の変数名（`--line-green` 等）はそのまま維持してリネーム差分を最小化
5. 移行後、カジさんから3点のフィードバックを受け、その場で反映・検証済み：
   - 「石垣牛」という具体的な文言を削除（キャンペーンセクションの見出しを匿名化）
   - キャンペーンセクション内の重複見出し「フォロワーキャンペーン」→「実施内容」に変更
   - 強調数値（`--num-accent`）の色を、当初のLINEグリーン `#06c755`（コントラスト不足）→ `#009e3a` に調整
   - グラフ数値ヒーロー部分の背景を薄緑塗りつぶし→白＋薄緑枠線に変更（カジさん指示「背景を白に」）
   - 月別詳細（年齢×性別グラフ）の男性バーの黒 `#1a1a1a` → 薄緑 `#8ecb96` に変更し、他グラフのトーンと統一

### 現在の主要デザイントークン（`instagram_ad_report.html` 15行目付近 `:root`）
| 変数名 | 値 | 用途 |
|---|---|---|
| `--line-green` | `#298737` | バー・枠線・塗り（primary-green-70、白地4.6:1） |
| `--line-green-soft` | `#e7f5e9` | チップ・淡い背景 |
| `--line-green-lite` | `#8ecb96` | 非キーの棒グラフ（男性バー等） |
| `--line-green-accent` | `#41ad4f` | ハイライトカードの outline |
| `--line-green-text` / `-strong` | `#237b31` / `#0f5c1f` | テキスト専用（5.3:1 / 9.0:1） |
| `--num-accent` | `#009e3a` | 強調数値専用（白背景3.53:1、灰背景3.26:1、**WCAG AA 3:1クリア**） |
| `--text-primary/secondary/tertiary` | `#08121a` 系 rgba | Spindleの3段階テキスト濃淡 |
| `--bg-secondary` | `#f5f6f6` | カード背景（グレー地） |
| `--font-base` | SF Pro JP / ヒラギノ角ゴ Pro 系 | 本文（フォントスタック変更は維持） |

### 検証済みの技術メモ
- `.card` は枠線+影 → グレー背景の平面デザインに変更。`.card.is-highlight` は塗りつぶしではなく `outline` で強調
- セクション見出しは「和文タイトル＋英字ラベル（Summary/Trends/Monthly data/Campaign/Conclusion）」の2段構成＋緑バーの縦線飾り
- Hero（ヘッダー）のブロブ装飾は元々なし（Spindle以前のデザインのまま）。h1を2段見出し化
- ブラウザでスクリーンショット・`getComputedStyle`による色値確認・DOM文言確認まで実施済み。GitHub Pagesへは**未push**

### 未検証・保留
- **印刷用CSS（`@media print`、旧939行目付近）は今回未確認のまま保留**（カジさんの指示で今回はスキップ）。`.card`が枠線を失った、`.chip`/`.hero-note`が枠線を失った、`.section-head`の構造が変わった、等の影響を次回確認する必要がある
- GitHub Pagesへの反映（push）は未実施

---

## 未解決：却下されたデザイン案（重要・再提案時の注意）

カジさんから添付された「ミヤマグラフィックス」風デザインシステム（アイボリー背景 `#F7F3EF`、
パステルカラー4色、大きな角丸、有機的ブロブ装飾、Zen Maru Gothicフォント、ピル型ボタン）を
参考にレイアウト変更を試みたが、**実装後にカジさんから明確に却下された**。却下理由は未確認。

- **次回このデザイン系統（アイボリー背景・パステル・丸ゴシック・ブロブ）を再提案しない**こと
- Spindle（本ドキュメント記載の現行デザイン）は正式採用済みなので、この制約とは無関係

---

## 次回セッション最初にすること
1. このファイルを読む
2. `git status` / `git log` で、この引き継ぎ更新コミットが最新であることを確認
3. 未検証タスクの扱いをカジさんに確認：
   - 印刷用CSS（`@media print`）の動作確認をやるか
   - GitHub Pagesへのpushを行うか（合意が出たら実行）
