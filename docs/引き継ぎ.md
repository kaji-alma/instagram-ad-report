# Instagram広告レポート プロジェクト 引き継ぎ

合言葉：「引き継ぎmdを読み再開」

## これは何か
注文住宅会社A社のInstagram広告運用ポートフォリオを、単一HTMLファイルのWebレポートにまとめて
GitHub Pagesで公開している。カジさんの実績紹介用（転職・営業ポートフォリオ想定）。

- 実ファイル：[instagram_ad_report.html](../instagram_ad_report.html)（リポジトリ直下）
- GitHubリポジトリ：`kaji-alma/instagram-ad-report`（public）
- 公開URL：https://kaji-alma.github.io/instagram-ad-report/instagram_ad_report.html
- 閲覧パスワード：`mirai`（sessionStorageのクライアント側チェック。ソース閲覧・DevToolsで突破可能なことは説明済み・カジさん承知の上）
- 元データ：`~/Downloads/ファインライフ・カンパニー沖縄様　広告レポート.xlsx`（16シート、15ヶ月＋サマリー）

※ この working directory（`My Post`）には `my-post/` や `podcast-pipeline/` など無関係な別プロジェクトも混在している。
このドキュメントは **instagram_ad_report.html のプロジェクトのみ** を対象とする。

## 確からしさ区分について
- **検証済み**：実際にブラウザ・GitHub Pagesで表示確認、またはgit historyで確認できる事実
- **未検証**：作成・記述しただけで動作未確認
- **未解決**：次に判断・着手すべきこと

---

## 検証済み：これまでの経緯

1. **本体作成**：Excelデータを基にレポートHTML生成（4種SVGチャート、15ヶ月分タブ、年齢×性別グラフ等）
2. **地名の匿名化**：物件名・地名（宮古島東仲宗根、石垣島真栄里等）がGoogle検索で会社特定に繋がるリスクがあったため、エリア名ベースの匿名化名に置換。会社名は最初から非掲載（「注文住宅会社A社」表記）
3. **パスワード保護overlay追加**：`mirai`固定。案件ごとに変更する運用も想定済み
4. **デザインリニューアル**（コミット `56ee5ed`、カジさん以外の作業 or 別セッションで実施）：WCAG AA準拠、ARIAタブ、セマンティックHTML、年ごとタブグループ化、印刷対応。現在の`.hero`構造（LINE風グリーン基調 `#06c755`、Noto Sans JP）はこのコミット由来
5. **ヘッダー説明文の追加**：14ヶ月の役割説明＋Claude Code使用の客観的注記を追加（該当行は下記参照）
6. **Claude Code注記の文言修正**：「データ集計・可視化・HTML生成」→「可視化・HTML生成」に変更（データ集計は自分でやった作業なので誤解を避けるため削除）
7. **フォントスタック変更**（コミット `885feba`、Canva/ClaudeDesignでの編集をダウンロード→リポジトリへ反映）：
   `--font-base`: SF Pro JP → ヒラギノ角ゴ Pro → Meiryo → MS Pゴシック
   `--font-num`: SF Pro Text → Helvetica Neue → Arial
8. **「月別推移」4グラフを2列→1列レイアウトに変更**（コミット `8a414a4`）：
   - `.charts-grid`（403行目付近）を `grid-template-columns: 1fr 1fr` → `1fr` に変更。1023px以下用の別メディアクエリも不要になり削除
   - 各グラフに数値ヒーローヘッダーを追加（`.chart-hero`：最新値を大きく表示＋変化率チップ「段階拡大／×47／↓70%／×2.6」＋開始値）
   - 棒・折れ線チャートの描画ロジック（`renderChart()`）に、開始点・ピーク・最新の3点だけ強調表示＋数値ラベルを出す機能を追加
   - CPC/CTRの数値誤差を修正：CPC 52円→**16円**だったのを**15円**に、CTR 2.1%→**5.4%**だったのを**5.5%**に（本文・summary-list・SVG descとも修正）
   - print用CSS（939行目〜）は元々 `.charts-grid` を2列固定にする別ルールがあり、そちらは維持（画面表示のみ1列化、印刷時は紙面節約のため2列のまま）
   - ブラウザでの表示確認済み（1列縦積み・ヒーローヘッダー・修正後数値とも正常に表示）
   - GitHub Pagesへpush済み（反映まで数分かかる場合あり）

### 現在のヘッダー文言（instagram_ad_report.html 983-995行付近）
```html
<div class="hero-eyebrow">Portfolio · 14ヶ月運用</div>
<h1>Instagram広告運用 <small>分析レポート</small></h1>
<p class="hero-lead">注文住宅会社A社のInstagramアカウントを<strong>ゼロから立ち上げ</strong>、
14ヶ月間の戦略設計・予算配分・PDCAサイクルの設計を担当しました。本レポートはその全期間データをまとめたものです。</p>
<div class="hero-meta">
  <span class="chip solid">14ヶ月運用</span>
  <span class="chip">2025年3月 – 2026年5月</span>
  <span class="chip">広告費 約116万円</span>
  <span class="chip">フォロワー 0 → 2,800</span>
</div>
<p class="hero-note">担当範囲：企画・戦略設計・A/Bテスト・予算配分。撮影・編集・広告ダッシュボード操作は
外部パートナーに委託し、ディレクション体制で運用しました。</p>
<p class="hero-note" style="font-size:0.82em; opacity:0.65;">
※ 本ページの可視化・HTML生成は Claude Code（Anthropic社のAIコーディングツール）を使用して作成しています。</p>
```

### git履歴（直近、`main`ブランチ、リモートpush済み）
```
8a414a4 月別推移グラフを2列→1列レイアウトに変更し、数値ヒーローヘッダーを追加
f11ab47 Add handoff doc for Instagram ad report project
885feba Update font stack to SF Pro / Hiragino / system fonts
cbf81bf Remove data aggregation from Claude Code attribution text
8b1eef5 Add project description and Claude Code attribution to header
56ee5ed Refresh report design: WCAG AA contrast, ARIA tabs, semantic HTML, year-grouped tabs, print support
9d20a35 Add password protection overlay
4eb1d18 Anonymize property location names for privacy
87ad01a Add Instagram ad report portfolio page
```
working tree はクリーン（未コミット差分なし）。`8a414a4` が最新でリモートと一致。

---

## 未解決：却下されたデザイン案（重要・再提案時の注意）

カジさんから添付された「ミヤマグラフィックス」風デザインシステム（アイボリー背景 `#F7F3EF`、
パステルカラー4色、大きな角丸、有機的ブロブ装飾、Zen Maru Gothicフォント、ピル型ボタン）を
参考にレイアウト変更を試みたが、**実装後にカジさんから明確に却下された**。

- 却下の経緯：「案B（トーン移植）」「Google Fontsのみでいい」まで合意を得て実装を進めたが、
  完成形を見て「このデザインシステムは却下します」との判断
- 却下の理由は聞けていない（**未解決**：次回、何が合わなかったかを確認すると次の方向性が立てやすい。
  例：ポップすぎた／信頼感が下がった／単に好みでなかった、等）
- 対応：未コミットの変更をすべて `git checkout` で破棄し、`885feba`（フォント変更まで）の状態に復元済み
- **次回このデザイン系統（アイボリー背景・パステル・丸ゴシック・ブロブ）を再提案しない**こと。
  別方向のデザイン案を出すか、カジさんの次の指示を待つ

---

## 次回セッション最初にすること
1. このファイルを読む
2. 現在のデザイン（LINE風グリーン基調、コミット`8a414a4`時点。月別推移4グラフは1列縦積み＋ヒーローヘッダー済み）が
   最新であることをgit logで再確認
3. カジさんに「デザイン変更の次の方向性」を確認してから着手（却下理由・別案の希望など）
   ※ この論点は「却下されたデザイン案」セクション（アイボリー/パステル系）に対するもので、
   直近の1列レイアウト変更（8a414a4）とは無関係
